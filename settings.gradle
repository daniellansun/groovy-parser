plugins {
    id "com.gradle.enterprise" version "3.6.4"
}

rootProject.name = 'groovy-parser'

gradleEnterprise {
    buildScan {
        termsOfServiceUrl = "https://gradle.com/terms-of-service"
        termsOfServiceAgree = "yes"
        publishAlways()
    }
}
